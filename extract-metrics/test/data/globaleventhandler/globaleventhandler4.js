window.onerror = function (message, source, lineno, colno, error) {
    throw message;
}

window.onerror = function (message, source, lineno, colno, error) {
    return file;
}

window.onerror = function (message, file, line, column, reason) {
    if(10 > 3) {
        throw reason;
    }
    console.log('error!');
}
